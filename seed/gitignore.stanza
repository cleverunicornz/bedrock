# --- bedrock: append to the consumer repo's .gitignore ---
# situation/ is repository law, not generated noise: every vertex is
# versioned and committed, and the root AGENTS.md — the compiled TriG
# graph — is committed at the repo root. NEVER ignore anything under
# situation/; if a file there looks generated, it is still law — commit it.
