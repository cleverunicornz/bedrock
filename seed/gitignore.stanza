# --- bedrock: append to the consumer repo's .gitignore ---
# situation/ is the complete canonical store: every resident or cold source
# is versioned and committed. Root AGENTS.md — the generated resident TriG
# projection — is committed too. NEVER ignore situation/; cold is a delivery
# state, not generated noise.
