# Required Tests

- The execution record must compile to deterministic TriG.
- The generated TriG must parse back to the exact RDF Dataset.
- Every retained negative control must return its declared refusal code.
